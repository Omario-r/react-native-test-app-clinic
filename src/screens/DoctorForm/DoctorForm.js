import React, { Component } from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';
import { Formik } from 'formik';

class DoctorForm extends Component {
  render() {
    return (
      <ScrollView>
        <Text>Form</Text>
      </ScrollView>
    );
  }
}
